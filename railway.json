{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install --include=dev && npx prisma generate && cd ../client && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma generate && node server/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
